<h2> Lista de Categorias: </h2>
<hr>
<a href="/categorias/add"><button class="btn btn-success">Nova Categoria </button></a>

{{#each categorias}}
<div class="card">
  <div class="card-body">
    <h4>{{name}}</h4>
    <small>{{slug}}</small>
  </div>
</div>
{{else}}

{{/each}}